export { useWindowSize } from './WindowContext';
export { WindowContext } from './WindowContext';
export { WindowContextProvider } from './WindowContext';

export interface WindowContextProps {
  windowHeight: number;
  windowWidth: number;
}
